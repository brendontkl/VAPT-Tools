# Core HTTP requests library
requests==2.31.0

# Alternative async-capable HTTP client (for future extensibility)
httpx==0.25.2

# For colored terminal output
colorama==0.4.6

# For JWT parsing and exploitation utilities
pyjwt==2.8.0

# For cryptographic signing/verification in JWT attacks
cryptography==41.0.7

# For modular arithmetic and RSA key recovery in JWT exploitation
gmpy2==2.1.5

# For SSL/TLS socket handling and certificate parsing
pyopenssl==23.2.0

# For parsing and validating URLs/HTML responses (used in header checks, smuggling response parsing)
beautifulsoup4==4.12.2